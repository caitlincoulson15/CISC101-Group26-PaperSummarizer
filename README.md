# CISC101-Group26-PaperSummarizer

This repo contains the system prompt and modules required for an AI paper summarizer. It instructs the LLM how to act internally and externally, by defining processes the user should and should not see.
